.DS_Store
.now/
dist/
node_modules/
target/
*.tsbuildinfo
.jest_cache

# Regenerated by `now-sdk dependencies` (instance-version specific, large)
@types/

# Local-machine Claude Code overrides
.claude/settings.local.json

# NEVER ignore the keys file: it maps Now.ID -> sys_id and must be committed,
# or CI mints fresh sys_ids and creates duplicate records on the instance.
!src/fluent/generated/keys.ts
